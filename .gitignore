node_modules
dist
.DS_Store
# .env is committed: it holds only public VITE_ values (Supabase URL +
# publishable key) that ship in the client bundle anyway, so every local
# build has the right config. Keep local overrides / any secrets in
# .env.local, which stays ignored.
.env.local
.env.*.local
*.log
.idea
.vscode
.cache
.wrangler
.claude

# Visual-review artifacts (local-only)
.screenshots/
_*.mjs
